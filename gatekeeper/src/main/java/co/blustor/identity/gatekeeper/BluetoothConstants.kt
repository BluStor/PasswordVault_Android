package co.blustor.identity.gatekeeper

class BluetoothConstants {

    enum class EventName {
        CHARACTERISTIC_READ, CHARACTERISTIC_WRITE, CONNECT, DESCRIPTOR_READ, DESCRIPTOR_WRITE, DISCONNECT, DISCOVER_SERVICES, NOTIFY, REQUEST_MTU
    }
}
